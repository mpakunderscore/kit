/* eslint-disable no-restricted-imports -- Shared contracts live under src; tsc does not rewrite path aliases for Node at runtime. */
export { DESKTOP_IPC_CHANNELS } from '../../src/shared/contracts/desktopIpc'
export { isUserPayload } from '../../src/shared/contracts/api'
